<h1><%= current_user.name %>'s Groups</h1>

</form>
<br>

<% if !@groups.empty? %>
  <% for group in @groups %>
    <%= render :partial => "group_box", :locals => {:group => group} %>
  <% end %>
  <%= will_paginate @groups, :class => "picnic_pagination"%>

<% else %>
<font size="2"> You are not a memeber of any groups.  Go to the <%= link_to "group search", :controller => "groups", :action => "index"%> to find one you might like.</font>
<% end %>


<br />