<div class="home_top">
<div class="what">Groups</div></div>
<font size="2">You can see all the food being eaten by members of these groups.
Enter group description to search (example: P90X Workout or Bank of America Employee's ):</font>
<%= form_tag "/groups/index" %>
<%= text_field "search", "query", {:size => 53 } %>
<%= submit_tag  "Search" %>
</form>
<% if flash[:notice] %><div class="error_box"><%= flash[:notice] %></div><% end %>
<br>
<br>
<%= link_to 'Create A New Group', new_group_path %><br><br>

<% if @groups != nil %>
  <% for group in @groups %>
    <%= render :partial => "group_box", :locals => {:group => group} %>
  <% end %>
  <%= will_paginate @groups, :class => "picnic_pagination", :params => {:q => @q}%>
<% end %>


<br />


